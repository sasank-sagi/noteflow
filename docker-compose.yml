services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - noteflow_data:/data
    environment:
      - DATABASE_PATH=/data/noteflow.db
    restart: unless-stopped

volumes:
  noteflow_data:
